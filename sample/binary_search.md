```
    int N, L;
    cin >> N >> L;
    int K;
    cin >> K;
    vec A(N);
    for (auto& a : A) {
        cin >> a;
    }

    int left = 0;
    int right = L;
    int mid = left + (right - left) / 2; 
    bool split = false;

    while (left <= right) {
        rep(i, N) {}

        if (split) {
        } else {
        }
    }
```